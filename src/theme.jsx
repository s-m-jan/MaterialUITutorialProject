import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette:{
        primary:{
            main: "#2c3e50",
            light: "skyblue"
        },
        secondary:{
            main: "#f00"
        },
        otherColor:{
            main: "#ccc"
        }
    } 
})